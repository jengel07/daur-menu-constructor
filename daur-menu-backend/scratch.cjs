const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const menus = await prisma.menu.findMany(); console.log('MENUS:', menus.map(m => ({ id: m.id, rest: m.restaurantId, itemsLen: m.items ? m.items.length : 0, catsLen: m.cats ? m.cats.length : 0 }))); const dishes = await prisma.dish.findMany(); console.log('DISHES:', dishes.length); } main().finally(() => prisma.$disconnect());
